import React from "react";

export const Projects = () =>{

    
    return (
        <nav className="projects segment">
            <h2>here will be a tab-bar with all the projects (kinda navbar)</h2>
        </nav>        
    )
}